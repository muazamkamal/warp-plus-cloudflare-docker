# warp-plus-cloudflare-docker
Docker Image for warp-plus-cloudflare

Docker wrapper for [this script](https://github.com/ALIILAPRO/warp-plus-cloudflare).

Supply the warp ID through environment variable `WARP_ID`.
